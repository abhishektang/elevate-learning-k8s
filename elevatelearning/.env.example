# Django Settings
DEBUG=False
SECRET_KEY=your-secret-key-here-change-in-production
ALLOWED_HOSTS=*

# Database Settings
DB_ENGINE=django.db.backends.mysql
DB_NAME=elevatelearning_db
DB_USER=djangouser
DB_PASSWORD=your-database-password-here
DB_HOST=db
DB_PORT=3306

# Security
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1

# Instructions:
# 1. Copy this file to .env
# 2. Update SECRET_KEY with a strong random string
# 3. Update DB_PASSWORD with your database password
# 4. Update CSRF_TRUSTED_ORIGINS with your actual domain/IP
# 5. Never commit the actual .env file to git!
